class FileProvider {
    constructor() {};

    /**
     * 
     * @param {*} objectPath 
     */
    loadObject(objectPath) {
        if (!objectPath) return null;
        let packagePath = objectPath;
    }

    
}

module.exports = FileProvider;